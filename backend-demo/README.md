1.Generate a package.json
    -npm init -y

2.create server.js

3.install , import "express" and create a http server after that assign port
    -npm i express

### connect MongoDB database
            REST API --->mongodb native driver--->  mongodb server
            REST API --->mongodb ODM tool---> mongodb server

    a.install mongoose and connect to mongodb server
    b.create schema of resource
    c.create model of that schema
    d.perform db operations on that model

### Error handling


### Running validators during update 

.gitignore